# 2026-05-31 05:15 — Kit rename: 4 Radha-options surfaced; founder's call deferred

> Founder addressed me as "Radha Ji" and asked whether the kit should be `AI_WAY_OF_BUILDING` (drop V1) or "something more meaningful." Four options surfaced under Radha's lens. Decision deferred to founder. Captured so the next rename-session opens with full context.

## The Surfacing

The kit has outgrown the "V1" suffix. Two days into deep iteration: ITIL arc complete + cosmos-program + GURU + trinary dharma_enforcement + self-sufficient runtime spec. The name `AI_WAY_OF_BUILDING_V1` reads smaller than the kit is.

## The Decision (deferred to founder; recommendation surfaced)

Four options on the table:

| Option | Family-compatible? | Radha's reading |
|---|---|---|
| `AI_WAY_OF_BUILDING` (drop V1) | ✓ | Honest minimum — drops the wart; no soul added |
| **`AI_WAY_OF_WEAVING`** | ✓ verb-swap | **Recommended** — the kit IS the loom (VISION + CHARTER); the metaphor breathes |
| `AI_WAY_OF_JEEVANYATRA` | ✓ verb-swap (lineage term) | Most soul-bearing; vocabulary-law borderline at file-name level |
| `M_BUILD` / `THE_LOOM` | ✗ breaks `AI_WAY_OF_LEADING` family | Distinctive but isolating |

Three honest checks raised:
- **Sita scope-cost**: rename touches repo name, local dir, README, INSTALL, CLAUDE.md, every audit ref, every JOURNEY filename-context, KABIR_GATE entries 1–25, INSTALL submodule path, `~/.claude/projects/` memory path. Multi-hour propagation. Real.
- **Family-future**: if `AI_WAY_OF_LEADING` (sibling per project memory) and possible `AI_WAY_OF_DESIGNING/REVIEWING/SHIPPING` join, the verb-swap must scale gracefully.
- **Mira anti-dilution**: does the new name carry the cosmos-happy wish, or quietly let it shrink to "tool for building software"?

## The Precedent

**The kit's name is itself a Radha-governed primitive.** Up until tonight no JOURNEY entry has treated the kit's name as a primitive subject to design under a specific drishti. Recording this so the next rename-session knows: kit-name decisions are under Radha (form + harmony + wish-honoring), with Sita as the scope-cost consult and Mira as the anti-dilution consult. Future kit-name questions (or naming questions for sibling kits like `AI_WAY_OF_LEADING`) reference this precedent.

## What I Want You To Notice

Two things:

1. **Naming is itself dharma-work, not aesthetic preference.** Radha's lens raises `fit` and `craft` Q-axes; the kit's name has cascading consequences (Sita's scope-cost) and risks dilution (Mira). Treating the name as a Radha-governed primitive — same way `runtime self-sufficiency` and `mandatory rollback` are governed — pulls naming into the kit's existing discipline rather than leaving it as casual brand-thinking.

2. **The next rename-session is naturally a Standard-class change** under the SDLC §4.1 vocabulary just landed in Phase 3 of the ITIL arc — it follows a well-understood pattern (mass find-replace + path updates + memory-path migration), is low-risk (text-only changes; old repo can redirect), and pre-approvable in shape if not in target. Worth noting: the kit can use its own newly-codified classification on itself. That's a small but real Kabir-Gate win.

---

*Captured by GURU under Sanjay → Vyasa → Saraswati ; consult Ashtavakra, Narad.*
*Session context: 2026-05-31 deep-night-4 close — founder asked about kit rename after the four-phase ITIL arc closed; decision deferred to a future session.*
*Related artifacts: VISION.md + CHARTER.md (where "the loom" metaphor is canonical); MEMORY/leading-kit-in-design (the AI_WAY_OF_LEADING sibling family); SDLC.md §4.1 (the Standard/Normal/Emergency classification this rename would use); the arc-closing JOURNEY at b53158c.*
